# Shared JWT Secret (use a strong, random secret in production)
JWT_SECRET=dev-super-secret-jwt-key-change-in-production

# Database Configuration
DATABASE_URL=postgresql://webrtc_user:webrtc_password@localhost:5433/webrtc_db
DB_HOST=localhost
DB_PORT=5433
DB_NAME=webrtc_db
DB_USER=webrtc_user
DB_PASSWORD=webrtc_password

# Service Ports
BACKEND_PORT=3001
SIGNALING_PORT=3002
FRONTEND_PORT=3000

# Rheomesh SFU Configuration
SFU_HOST=localhost
SFU_PORT=8080
SFU_SIGNALING_PORT=8081

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Node Environment
NODE_ENV=development

# Rust Environment
RUST_LOG=info
