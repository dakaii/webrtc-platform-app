# Security (CHANGE IN PRODUCTION!)
JWT_SECRET=dev-super-secret-jwt-key-change-in-production

# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=webrtc_db
DB_USER=webrtc_user
DB_PASSWORD=webrtc_password

# Redis Configuration (for Signaling Cluster)
REDIS_PASSWORD=dev-redis-password
REDIS_PORT=6379

# Service Ports
BACKEND_PORT=4000
SIGNALING_PORT=9000
FRONTEND_PORT=8080

# Clustering Configuration
CLUSTER_MODE=true
NODE_ID=signaling-1

# Development Environment
NODE_ENV=development
RUST_LOG=info
